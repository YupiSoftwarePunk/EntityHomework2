<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ListBox Name="mainListBox" Grid.Row="0" Grid.Column="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="2">
                        <TextBlock Text="{Binding Path=Title}"/>
                        <TextBlock Text="{Binding Path=Company}"/>
                        <TextBlock Text="{Binding Path=Price}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="0" Grid.Column="1">
            <TextBlock Text="Выбранный элемент" Margin="5"/>
            <TextBlock Text="Модель" VerticalAlignment="Center"/>
            <TextBox x:Name="ModelInput"/>

            <TextBlock Text="Производитель" VerticalAlignment="Center"/>
            <TextBox x:Name="BrandInput"/>

            <TextBlock Text="Цена" VerticalAlignment="Center"/>
            <TextBox x:Name="PriceInput"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
            <Button x:Name="Plus" Content="+" Height="80" Width="60" 
                    VerticalAlignment="Stretch" HorizontalAlignment="Left" 
                    Margin="10" Click="AddClick"/>
            <Button x:Name="Minus" Content="-" Height="80" Width="60" 
                    VerticalAlignment="Stretch" HorizontalAlignment="Right" 
                    Margin="10" Click="RemoveClick"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" >
            <TextBlock Text="Ошибки" Margin="5"/>
            <Border x:Name="ErrorBorder">
                <TextBlock x:Name="ErrorMessage" Foreground="Red" FontWeight="SemiBold"
                           TextWrapping="Wrap" TextAlignment="Center"/>
            </Border>
        </StackPanel>

    </Grid>
</Window>
